父任务：
{parent_description}

所有子任务已完成。子任务列表：
{children_summary}

{interface_contract_section}

{context_section}

请验证所有子模块协作正确。你可以使用 shell、write_file、read_file、list_dir 工具。

集成验证流程（高效执行，避免不必要的 read_file）：
1. 直接运行主程序/入口脚本，检查输出是否正确
2. 如果有 output/ 目录的结果文件，检查其内容
3. 只有在运行报错时才 read_file 排查问题
4. 通过后调用 task_done 声明完成
