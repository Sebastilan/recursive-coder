当前任务：
{task_description}

{ancestry_section}

{execution_plan_section}

{interface_section}

验证标准：
- 说明：{verification_description}
- 预期输出：{expected_output}
- 验证命令：{verification_command}

{context_section}

请严格按照上面的执行计划完成此任务。你可以使用以下工具：
- shell：执行终端命令（编译、运行、安装依赖、查看文件等）
- write_file：写入文件
- read_file：读取文件
- list_dir：查看目录内容

工作流程：
1. 先按执行计划的步骤逐步实现
2. 实现时严格遵守接口定义（函数签名、输入输出格式）
3. 用 shell 运行验证命令，检查是否通过
4. 如果验证失败，根据错误信息修改代码
5. 验证通过后，调用 task_done 声明完成

注意：
- 所有文件路径都相对于工作目录
- 严格按照计划的步骤顺序执行，不要跳步
- 输出格式必须严格符合接口定义
- 确保验证命令通过后再声明完成
